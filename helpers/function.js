
function addSuhu(fullName) {
   return `Suhu ${fullName}`
}

function getFullName(first, second) {
   let fullName = `${first} ${second}`

   return fullName
}

module.exports = {addSuhu, getFullName}